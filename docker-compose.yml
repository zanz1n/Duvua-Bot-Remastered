version: '3.7'

services:
  nodebot-deploy:
    image: node:latest
    working_dir: /home/node/app
    container_name: node_bot_compose
    command: /bin/sh -c "yarn install && yarn start"
    volumes:
      - ./:/home/node/app
    restart: always
